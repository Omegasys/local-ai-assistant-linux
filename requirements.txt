# requirements.txt
flask==2.1.1  # Example: Flask for web-based features (if applicable)
psutil==5.9.0  # System monitoring (e.g., CPU, memory usage)
SpeechRecognition==3.8.1  # Speech recognition library
pyttsx3==2.90  # Text-to-speech (if applicable)
cryptography==36.0.1  # For data encryption
nltk==3.7  # For natural language processing (e.g., tokenization, stopwords)
scikit-learn==0.24.2  # For machine learning (e.g., intent recognition)
numpy==1.21.0  # Numerical computing (if needed for AI models)
pillow==8.4.0  # For image handling (if needed)
requests==2.26.0  # For HTTP requests (if applicable for web search integration)
Flask-Cors==3.1.1  # For CORS support in Flask (if applicable)
